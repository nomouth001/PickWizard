# 알고리즘 재설계 명세서 - 패턴 분석 편

**작성일**: 2026-01-07  
**목적**: Trial04/Trial05 패턴 분석 아이디어 기반 통합 알고리즘 설계  
**참조**: 010.1(빈도), 010.2(LSTM) - 일관된 통합 철학

---

## 📋 설계 원칙

### 1. 메타 패턴 분석의 핵심
- **숫자가 아닌 패턴을 학습**: 범위/순위의 메타 정보 활용
- **도메인 지식 기반**: 로또 번호 분포 특성 반영
- **특징 공학 (Feature Engineering)**: 원본 숫자를 의미 있는 특징으로 변환

### 2. 통합 철학 유지
- **하나의 알고리즘, 여러 패턴 타입**
- 프리셋 + 커스텀 지원
- 빈도/LSTM 알고리즘과 일관된 인터페이스

### 3. Trial04/Trial05 핵심 보존
- **범위 패턴 (Range Pattern)**: ABCDE 5구간 분할
- **순위 패턴 (Rank Pattern)**: 출현 빈도 순위 조합
- **역확률 샘플링**: 패턴 내에서도 적용 가능

---

## 🎯 알고리즘 체계

### Phase 1: 통합 전 구조 (기존)

| Trial | 패턴 타입 | 설명 |
|-------|----------|------|
| Trial04 | 범위 패턴 | 1~45를 A~E 5구간으로 분할, 패턴 빈도 분석 |
| Trial05 | 순위 패턴 | 출현 빈도 순위의 조합 패턴 분석 |

**문제점**: 두 가지 유사한 접근이 분리되어 있음

---

### Phase 2: 통합 후 구조 (제안) ⭐

#### **알고리즘 4: 패턴 분석 (Advanced Pattern)**

**설명**: 로또 번호를 메타 패턴으로 변환하여 과거 출현 빈도가 높은 패턴을 기반으로 번호를 생성하는 특징 공학 기반 알고리즘

**핵심 차별화 요소**:
1. **패턴 타입 선택** (Range vs Rank)
2. **패턴 크기 조정** (구간 개수, 순위 조합 크기)
3. **패턴 내 확률 분포** (균등 vs 빈도 기반 vs 역확률)
4. **역확률 샘플링** (패턴 선택 + 번호 선택 2단계)

**파라미터 조합**:
- `패턴=Range + 5구간 + 빈도 기반` → Trial04 기본
- `패턴=Range + 커스텀 구간 + 역확률` → 고급 범위 패턴
- `패턴=Rank + 3개 조합 + 빈도 기반` → Trial05 기본
- `패턴=Rank + 커스텀 조합 + 역확률` → 고급 순위 패턴

**비용**: 2~4코인 (패턴 타입 + 복잡도)

---

## 📊 패턴 타입 상세 설명

### 1. 범위 패턴 (Range Pattern) - Trial04 기반

#### 1.1 개념

**정의**: 1~45 번호를 N개 구간으로 분할하고, 각 구간을 문자로 표현

**기본 구조 (5구간)**:
```
A: 1~9    (9개)
B: 10~18  (9개)
C: 19~27  (9개)
D: 28~36  (9개)
E: 37~45  (9개)
```

**패턴 예시**:
```
번호: [5, 12, 23, 31, 38, 42]
패턴: [A, B, C, D, E, E] → 정렬 → "ABCDEE"
```

**특징**:
- ✅ 번호 분포의 균형 분석
- ✅ 고/저 번호 비율 자동 반영
- ✅ 직관적 이해 가능
- ❌ 구간 경계 번호 처리 민감

---

#### 1.2 구간 설정 옵션

**A. 기본 5구간 (ABCDE)**
- 각 구간 9개씩 균등 분할
- Trial04 원본 방식

**B. 커스텀 3구간 (Low-Mid-High)**
```
Low:  1~15  (15개)
Mid:  16~30 (15개)
High: 31~45 (15개)
```

**C. 커스텀 2구간 (Low-High)**
```
Low:  1~22  (22개)
High: 23~45 (23개)
```

**D. 세밀한 10구간**
```
A: 1~5
B: 6~10
C: 11~15
...
J: 41~45
```

**UI 예시**:
```
┌─────────────────────────────────────┐
│ 📐 범위 구간 설정                     │
├─────────────────────────────────────┤
│ ● 5구간 (ABCDE) ⭐                   │
│   A:1~9, B:10~18, C:19~27,          │
│   D:28~36, E:37~45                  │
│                                     │
│ ○ 3구간 (Low-Mid-High)              │
│   각 15개씩                          │
│                                     │
│ ○ 2구간 (Low-High)                  │
│   1~22, 23~45                       │
│                                     │
│ ○ 10구간 (세밀)                     │
│   각 4~5개씩                         │
│                                     │
│ ○ 커스텀 구간                        │
│   [구간1: 1~__] [구간2: __~__] ...  │
└─────────────────────────────────────┘
```

---

#### 1.3 범위 패턴 분석 프로세스

**1단계: 과거 데이터 패턴 변환**
```python
def numbers_to_range_pattern(numbers, ranges):
    """
    번호 → 범위 패턴 변환
    
    Args:
        numbers: [5, 12, 23, 31, 38, 42]
        ranges: {'A': (1,9), 'B': (10,18), ...}
    
    Returns:
        "ABCDEE"
    """
    pattern = []
    for num in numbers:
        for label, (start, end) in ranges.items():
            if start <= num <= end:
                pattern.append(label)
                break
    
    return ''.join(sorted(pattern))
```

**2단계: 패턴 빈도 집계**
```python
def analyze_range_patterns(historical_data, ranges):
    """
    과거 회차의 패턴 빈도 분석
    
    Returns:
        Counter({'ABCDEE': 41, 'ABCCDE': 38, ...})
    """
    patterns = []
    
    for idx, row in historical_data.iterrows():
        numbers = [row[f'num{i}'] for i in range(1, 7)]
        pattern = numbers_to_range_pattern(numbers, ranges)
        patterns.append(pattern)
    
    return Counter(patterns)
```

**3단계: 상위 패턴 선택**
```python
def get_top_patterns(pattern_counter, n=10):
    """상위 N개 패턴 추출"""
    return [pat for pat, _ in pattern_counter.most_common(n)]
```

**4단계: 패턴 기반 번호 생성**
```python
def generate_from_range_pattern(
    pattern, 
    ranges, 
    frequency_dict,
    probability_mode='normal'
):
    """
    패턴에서 실제 번호 생성
    
    Args:
        pattern: "ABCDEE"
        ranges: 구간 정의
        frequency_dict: 각 번호의 출현 빈도
        probability_mode: 'normal' | 'inverse'
    
    Returns:
        [5, 12, 23, 31, 38, 42]
    """
    selected = []
    
    for symbol in pattern:
        # 해당 구간의 번호 풀
        start, end = ranges[symbol]
        pool = [n for n in range(start, end+1) 
                if n not in selected]
        
        if not pool:
            continue
        
        # 확률 계산
        if probability_mode == 'normal':
            # 빈도 기반
            weights = [frequency_dict.get(n, 1) for n in pool]
        else:  # 'inverse'
            # 역확률
            max_freq = max(frequency_dict.get(n, 1) for n in pool)
            weights = [max_freq - frequency_dict.get(n, 1) + 1 
                       for n in pool]
        
        # 정규화
        total = sum(weights)
        probs = [w / total for w in weights]
        
        # 샘플링
        chosen = np.random.choice(pool, size=1, p=probs)[0]
        selected.append(chosen)
    
    return sorted(selected)
```

---

### 2. 순위 패턴 (Rank Pattern) - Trial05 기반

#### 2.1 개념

**정의**: 누적 출현 빈도로 각 번호의 순위를 계산하고, 당첨 번호의 순위 조합 패턴을 분석

**예시**:
```
# 누적 빈도 (1~1205회차)
12번: 150회 → 1위
34번: 148회 → 2위
23번: 145회 → 3위
...
7번: 120회 → 45위

# 1206회차 당첨번호
번호: [12, 23, 34, ...]
순위: [1, 3, 2, ...]

# 순위 패턴 (3개 조합)
combinations(3): (1, 2, 3), (1, 2, 4), ...
```

**특징**:
- ✅ 숫자가 아닌 "상대적 인기도" 분석
- ✅ 메타 패턴 (pattern of patterns)
- ✅ 빈도 편향 자동 보정
- ❌ 계산 복잡도 높음

---

#### 2.2 순위 계산 방식

**A. 누적 출현 빈도 순위 (기본)**
```python
def calculate_cumulative_ranks(historical_data):
    """
    누적 출현 빈도로 순위 계산
    
    Returns:
        {회차: {번호: 순위}}
    """
    ranks_per_round = {}
    cumulative_freq = Counter()
    
    for idx, row in historical_data.iterrows():
        # 이전 회차까지의 누적 빈도로 순위 계산
        sorted_nums = sorted(
            cumulative_freq.items(),
            key=lambda x: (-x[1], x[0])  # 빈도 내림차순, 번호 오름차순
        )
        rank_dict = {num: rank+1 for rank, (num, _) in enumerate(sorted_nums)}
        
        # 0회 출현 번호는 46위로 처리
        for num in range(1, 46):
            if num not in rank_dict:
                rank_dict[num] = 46
        
        ranks_per_round[idx] = rank_dict
        
        # 현재 회차 번호 누적
        for i in range(1, 7):
            cumulative_freq[row[f'num{i}']] += 1
    
    return ranks_per_round
```

**B. 최근 N회차 빈도 순위**
```python
def calculate_recent_ranks(historical_data, window_size=50):
    """최근 N회차 기준 순위"""
    ranks_per_round = {}
    
    for idx in range(len(historical_data)):
        start = max(0, idx - window_size)
        recent = historical_data.iloc[start:idx]
        
        freq = Counter()
        for _, row in recent.iterrows():
            for i in range(1, 7):
                freq[row[f'num{i}']] += 1
        
        sorted_nums = sorted(freq.items(), key=lambda x: (-x[1], x[0]))
        rank_dict = {num: rank+1 for rank, (num, _) in enumerate(sorted_nums)}
        
        for num in range(1, 46):
            if num not in rank_dict:
                rank_dict[num] = 46
        
        ranks_per_round[idx] = rank_dict
    
    return ranks_per_round
```

---

#### 2.3 순위 패턴 분석 프로세스

**1단계: 각 회차의 순위 패턴 계산**
```python
def get_rank_patterns(historical_data, ranks_per_round, combo_size=3):
    """
    각 회차의 당첨번호를 순위 조합으로 변환
    
    Args:
        combo_size: 조합 크기 (3, 4, 5, 6)
    
    Returns:
        [회차별 순위 조합 리스트]
    """
    rank_patterns = []
    
    for idx, row in historical_data.iterrows():
        numbers = [row[f'num{i}'] for i in range(1, 7)]
        
        # 순위 매핑
        rank_dict = ranks_per_round[idx]
        ranks = [rank_dict.get(num, 46) for num in numbers]
        
        # N개 조합 생성
        combos = combinations(sorted(ranks), combo_size)
        rank_patterns.append(list(combos))
    
    return rank_patterns
```

**2단계: 패턴 빈도 집계**
```python
def analyze_rank_patterns(rank_patterns):
    """
    순위 조합 패턴의 출현 빈도 분석
    
    Returns:
        Counter({(1,2,3): 15, (1,3,5): 12, ...})
    """
    pattern_counter = Counter()
    
    for patterns in rank_patterns:
        for combo in patterns:
            pattern_counter[combo] += 1
    
    return pattern_counter
```

**3단계: 상위 패턴 선택**
```python
def get_top_rank_patterns(pattern_counter, n=10):
    """상위 N개 순위 패턴 추출"""
    return [pat for pat, _ in pattern_counter.most_common(n)]
```

**4단계: 패턴 기반 번호 생성**
```python
def generate_from_rank_pattern(
    rank_combo,
    rank_dict,
    frequency_dict,
    probability_mode='normal'
):
    """
    순위 조합에서 실제 번호 생성
    
    Args:
        rank_combo: (1, 2, 3) - 목표 순위 조합
        rank_dict: {번호: 순위}
        frequency_dict: {번호: 빈도}
        probability_mode: 'normal' | 'inverse'
    
    Returns:
        [12, 23, 34, ...] (6개)
    """
    selected = []
    
    # 1. 순위 조합에 해당하는 번호 선택
    reverse_rank = {rank: num for num, rank in rank_dict.items()}
    
    for target_rank in rank_combo:
        if target_rank in reverse_rank:
            selected.append(reverse_rank[target_rank])
    
    # 2. 나머지 번호 추가 (빈도 기반)
    remaining = 6 - len(selected)
    
    if remaining > 0:
        # 전체 번호 중 미선택 번호
        pool = [n for n in range(1, 46) if n not in selected]
        
        # 확률 계산
        if probability_mode == 'normal':
            weights = [frequency_dict.get(n, 1) for n in pool]
        else:  # 'inverse'
            max_freq = max(frequency_dict.get(n, 1) for n in pool)
            weights = [max_freq - frequency_dict.get(n, 1) + 1 
                       for n in pool]
        
        total = sum(weights)
        probs = [w / total for w in weights]
        
        chosen = np.random.choice(pool, size=remaining, 
                                  replace=False, p=probs)
        selected.extend(chosen.tolist())
    
    return sorted(selected)
```

---

## 🎨 파라미터 구조

### A. 패턴 타입 (Pattern Type) ⭐

**설명**: 메타 패턴의 종류 선택

**옵션**:

#### **A-1. 범위 패턴 (Range Pattern)**

**정의**: 번호 범위를 구간으로 분할

**UI 라벨**: "범위 패턴 (ABCDE)"

---

#### **A-2. 순위 패턴 (Rank Pattern)**

**정의**: 출현 빈도 순위의 조합

**UI 라벨**: "순위 패턴 (빈도 순위)"

---

**UI 예시**:
```
┌─────────────────────────────────────┐
│ 🎯 패턴 타입                          │
├─────────────────────────────────────┤
│ ● 범위 패턴 (Range Pattern)          │
│   번호를 구간으로 분할                │
│   예: [5,12,23,31,38,42] → ABCDEE   │
│                                     │
│ ○ 순위 패턴 (Rank Pattern)           │
│   출현 빈도 순위의 조합                │
│   예: [1위,3위,5위] 조합 패턴         │
└─────────────────────────────────────┘
```

---

### B. 패턴 설정 (Pattern Configuration)

#### **B-1. 범위 패턴 설정**

**파라미터**:
- **구간 개수**: 2, 3, 5 ⭐, 10
- **커스텀 구간**: 직접 입력

**UI 예시**:
```
┌─────────────────────────────────────┐
│ 📐 범위 구간 설정                     │
├─────────────────────────────────────┤
│ ○ 2구간 (Low-High)                  │
│ ○ 3구간 (Low-Mid-High)              │
│ ● 5구간 (ABCDE) ⭐                   │
│ ○ 10구간 (세밀)                     │
│ ○ 커스텀                             │
└─────────────────────────────────────┘
```

---

#### **B-2. 순위 패턴 설정**

**파라미터**:
- **조합 크기**: 3 ⭐, 4, 5, 6
- **순위 계산 방식**: 누적 빈도 ⭐, 최근 N회차
- **윈도우 크기** (최근 N회차 선택 시): 20, 50 ⭐, 100

**UI 예시**:
```
┌─────────────────────────────────────┐
│ 📊 순위 패턴 설정                     │
├─────────────────────────────────────┤
│ 조합 크기:                           │
│ ● 3개 ⭐  ○ 4개  ○ 5개  ○ 6개      │
│                                     │
│ 순위 계산 방식:                       │
│ ● 누적 빈도 (전체 회차)               │
│ ○ 최근 N회차                         │
│   Window: [50] 회차                 │
└─────────────────────────────────────┘
```

---

### C. 분석 범위 (Analysis Window)

**설명**: 패턴 빈도 분석에 사용할 과거 회차 범위

**옵션**:
- **전체 회차** (기본값)
- **최근 N회차**: 10 ~ 500

**UI 예시**:
```
┌─────────────────────────────────────┐
│ 📅 분석 범위                          │
├─────────────────────────────────────┤
│ ● 전체 회차 (1205회차)               │
│ ○ 최근 N회차                         │
│   [50] [100] [200]  또는 [___]      │
└─────────────────────────────────────┘
```

---

### D. 상위 패턴 개수 (Top N Patterns)

**설명**: 사용할 상위 패턴 개수

**옵션**: 5, 10 ⭐, 20, 30

**UI 예시**:
```
┌─────────────────────────────────────┐
│ 🔝 상위 패턴 개수                     │
├─────────────────────────────────────┤
│ ○ 5개   ● 10개 ⭐  ○ 20개  ○ 30개  │
└─────────────────────────────────────┘
```

---

### E. 패턴 내 확률 분포 (In-Pattern Probability)

**설명**: 패턴에서 실제 번호를 선택하는 방식

**옵션**:

#### **E-1. 균등 확률 (Uniform)**
- 구간/순위 내 모든 번호 동일 확률

#### **E-2. 빈도 기반 (Frequency)**
- 출현 빈도 높은 번호 우선

#### **E-3. 역확률 (Inverse)** ⭐
- 출현 빈도 낮은 번호 우선

**UI 예시**:
```
┌─────────────────────────────────────┐
│ 🎲 패턴 내 번호 선택 방식              │
├─────────────────────────────────────┤
│ ○ 균등 확률                          │
│   구간/순위 내 모든 번호 동일 확률     │
│                                     │
│ ● 빈도 기반                          │
│   출현 빈도 높은 번호 우선             │
│                                     │
│ ○ 역확률 ⭐                          │
│   출현 빈도 낮은 번호 우선             │
│   (평균 회귀 전략)                    │
└─────────────────────────────────────┘
```

---

### F. 패턴 선택 확률 (Pattern Selection Probability)

**설명**: 어떤 패턴을 선택할지 결정하는 방식

**옵션**:

#### **F-1. 정확률 (Normal)**
- 빈도 높은 패턴 우선 선택

#### **F-2. 역확률 (Inverse)** ⭐
- 빈도 낮은 패턴 우선 선택

**UI 예시**:
```
┌─────────────────────────────────────┐
│ 📈 패턴 선택 방식                     │
├─────────────────────────────────────┤
│ ● 정확률                             │
│   빈도 높은 패턴 우선                 │
│                                     │
│ ○ 역확률 ⭐                          │
│   빈도 낮은 패턴 우선                 │
│   (덜 나온 패턴 선호)                 │
└─────────────────────────────────────┘
```

---

## 🎯 프리셋 조합

### 기본 프리셋 (6개)

| 프리셋 이름 | 패턴 타입 | 설정 | 파라미터 | 비용 |
|------------|----------|------|---------|------|
| **범위 기본** | Range | 5구간, 빈도 기반 | `type='range', divisions=5, prob='frequency'` | 2코인 |
| **범위 역확률** ⭐ | Range | 5구간, 역확률 | `type='range', divisions=5, prob='inverse'` | 3코인 |
| **순위 기본** | Rank | 3개 조합, 빈도 기반 | `type='rank', combo=3, prob='frequency'` | 2코인 |
| **순위 역확률** ⭐ | Rank | 3개 조합, 역확률 | `type='rank', combo=3, prob='inverse'` | 3코인 |
| **범위 세밀** | Range | 10구간, 역확률 | `type='range', divisions=10, prob='inverse'` | 4코인 |
| **순위 고급** | Rank | 4개 조합, 역확률 | `type='rank', combo=4, prob='inverse'` | 4코인 |

---

## 💰 비용 정책

### 기본 비용 산정

| 패턴 타입 | 확률 방식 | 기본 비용 |
|----------|----------|---------|
| Range (2~5구간) | Frequency | 2코인 |
| Range (2~5구간) | Inverse | 3코인 |
| Range (10구간) | Any | +1코인 |
| Rank (3개 조합) | Frequency | 2코인 |
| Rank (3개 조합) | Inverse | 3코인 |
| Rank (4~6개 조합) | Any | +1코인 |

---

## 📝 완전 구현 코드

### 통합 파라미터 구조

```python
class PatternParams:
    # A. 패턴 타입
    pattern_type: Literal['range', 'rank'] = 'range'
    
    # B-1. 범위 패턴 설정
    range_divisions: int = 5  # 2, 3, 5, 10
    custom_ranges: Optional[Dict[str, tuple]] = None  # 커스텀 구간
    
    # B-2. 순위 패턴 설정
    rank_combo_size: int = 3  # 3, 4, 5, 6
    rank_mode: Literal['cumulative', 'recent'] = 'cumulative'
    rank_window: int = 50  # recent일 때만
    
    # C. 분석 범위
    analysis_window_type: Literal['all', 'recent'] = 'all'
    analysis_window_size: Optional[int] = None
    
    # D. 상위 패턴 개수
    top_n_patterns: int = 10  # 5, 10, 20, 30
    
    # E. 패턴 내 확률 분포
    in_pattern_probability: Literal['uniform', 'frequency', 'inverse'] = 'frequency'
    
    # F. 패턴 선택 확률
    pattern_selection_probability: Literal['normal', 'inverse'] = 'normal'
```

---

### 통합 알고리즘 클래스

```python
"""
Algorithm 4: 패턴 분석 (통합 버전)

범위 패턴 × 순위 패턴 = 하나로 통합

2026-01-07 EST - 초기 설계 (통합 버전)
"""

import numpy as np
import pandas as pd
from typing import List, Dict, Optional, Any, Literal, Tuple
from collections import Counter
from itertools import combinations

from app.algorithms.base import LottoAlgorithm


# =====================================
# 범위 패턴 유틸리티
# =====================================

def get_default_ranges(divisions: int) -> Dict[str, Tuple[int, int]]:
    """
    기본 구간 정의
    
    Args:
        divisions: 2, 3, 5, 10
    
    Returns:
        {'A': (1, 9), 'B': (10, 18), ...}
    """
    if divisions == 2:
        return {
            'L': (1, 22),   # Low
            'H': (23, 45)   # High
        }
    elif divisions == 3:
        return {
            'L': (1, 15),   # Low
            'M': (16, 30),  # Mid
            'H': (31, 45)   # High
        }
    elif divisions == 5:
        return {
            'A': (1, 9),
            'B': (10, 18),
            'C': (19, 27),
            'D': (28, 36),
            'E': (37, 45)
        }
    elif divisions == 10:
        labels = 'ABCDEFGHIJ'
        ranges = {}
        for i, label in enumerate(labels):
            start = i * 5 + 1
            end = min((i + 1) * 5, 45)
            ranges[label] = (start, end)
        return ranges
    else:
        raise ValueError(f"Unsupported divisions: {divisions}")


def numbers_to_range_pattern(
    numbers: List[int],
    ranges: Dict[str, Tuple[int, int]]
) -> str:
    """번호 → 범위 패턴 변환"""
    pattern = []
    
    for num in numbers:
        for label, (start, end) in ranges.items():
            if start <= num <= end:
                pattern.append(label)
                break
    
    return ''.join(sorted(pattern))


# =====================================
# 순위 패턴 유틸리티
# =====================================

def calculate_frequency_ranks(
    historical_data: pd.DataFrame,
    mode: str = 'cumulative',
    window_size: int = 50
) -> Dict[int, Dict[int, int]]:
    """
    빈도 기반 순위 계산
    
    Args:
        mode: 'cumulative' | 'recent'
    
    Returns:
        {회차_idx: {번호: 순위}}
    """
    ranks_per_round = {}
    
    if mode == 'cumulative':
        # 누적 빈도
        cumulative_freq = Counter()
        
        for idx, row in historical_data.iterrows():
            # 현재까지의 누적 빈도로 순위 계산
            if len(cumulative_freq) > 0:
                sorted_nums = sorted(
                    cumulative_freq.items(),
                    key=lambda x: (-x[1], x[0])
                )
                rank_dict = {num: rank+1 for rank, (num, _) in enumerate(sorted_nums)}
            else:
                rank_dict = {}
            
            # 0회 출현 번호는 46위
            for num in range(1, 46):
                if num not in rank_dict:
                    rank_dict[num] = 46
            
            ranks_per_round[idx] = rank_dict
            
            # 현재 회차 누적
            for i in range(1, 7):
                cumulative_freq[row[f'num{i}']] += 1
    
    else:  # 'recent'
        for idx in range(len(historical_data)):
            start = max(0, idx - window_size)
            recent = historical_data.iloc[start:idx]
            
            freq = Counter()
            for _, row in recent.iterrows():
                for i in range(1, 7):
                    freq[row[f'num{i}']] += 1
            
            if len(freq) > 0:
                sorted_nums = sorted(freq.items(), key=lambda x: (-x[1], x[0]))
                rank_dict = {num: rank+1 for rank, (num, _) in enumerate(sorted_nums)}
            else:
                rank_dict = {}
            
            for num in range(1, 46):
                if num not in rank_dict:
                    rank_dict[num] = 46
            
            ranks_per_round[idx] = rank_dict
    
    return ranks_per_round


# =====================================
# 통합 알고리즘 클래스 ⭐
# =====================================

class AdvancedPatternAlgorithm(LottoAlgorithm):
    """
    알고리즘 4: 패턴 분석 (통합 버전)
    
    범위 패턴 × 순위 패턴
    """
    
    def __init__(self):
        super().__init__(
            algorithm_id=4,
            name="패턴 분석 (Advanced Pattern)",
            description="메타 패턴 분석 (범위/순위)",
            cost_per_set=2
        )
    
    def generate_numbers(
        self,
        historical_data: Optional[pd.DataFrame],
        n_sets: int = 5,
        exclude_numbers: Optional[List[int]] = None,
        include_numbers: Optional[List[int]] = None,
        
        # A. 패턴 타입 ⭐
        pattern_type: Literal['range', 'rank'] = 'range',
        
        # B-1. 범위 패턴 설정
        range_divisions: int = 5,
        custom_ranges: Optional[Dict[str, Tuple[int, int]]] = None,
        
        # B-2. 순위 패턴 설정
        rank_combo_size: int = 3,
        rank_mode: Literal['cumulative', 'recent'] = 'cumulative',
        rank_window: int = 50,
        
        # C. 분석 범위
        analysis_window_type: Literal['all', 'recent'] = 'all',
        analysis_window_size: Optional[int] = None,
        
        # D. 상위 패턴 개수
        top_n_patterns: int = 10,
        
        # E. 패턴 내 확률 분포
        in_pattern_probability: Literal['uniform', 'frequency', 'inverse'] = 'frequency',
        
        # F. 패턴 선택 확률
        pattern_selection_probability: Literal['normal', 'inverse'] = 'normal',
        
        **kwargs
    ) -> List[List[int]]:
        """패턴 분석 번호 생성 (통합 버전)"""
        
        # 파라미터 검증
        valid, error_msg = self.validate_parameters(
            n_sets, exclude_numbers, include_numbers
        )
        if not valid:
            raise ValueError(error_msg)
        
        if historical_data is None or len(historical_data) < 10:
            raise ValueError("최소 10회차 이상의 데이터 필요")
        
        # 분석 범위 설정
        if analysis_window_type == 'all':
            analysis_data = historical_data
        else:
            analysis_data = historical_data.tail(analysis_window_size or 100)
        
        # ===== 1. 패턴 타입에 따라 분기 =====
        
        if pattern_type == 'range':
            # 범위 패턴
            patterns, pattern_generator = self._analyze_range_patterns(
                analysis_data, historical_data,
                range_divisions, custom_ranges,
                top_n_patterns, pattern_selection_probability
            )
        else:  # 'rank'
            # 순위 패턴
            patterns, pattern_generator = self._analyze_rank_patterns(
                analysis_data, historical_data,
                rank_combo_size, rank_mode, rank_window,
                top_n_patterns, pattern_selection_probability
            )
        
        # ===== 2. 빈도 계산 (패턴 내 번호 선택용) =====
        frequency_dict = self._calculate_frequency(analysis_data)
        
        # ===== 3. 번호 생성 =====
        results = []
        
        for _ in range(n_sets):
            # 패턴 선택
            selected_pattern = patterns[_ % len(patterns)]
            
            # 패턴 → 번호 변환
            numbers = pattern_generator(
                selected_pattern,
                frequency_dict,
                in_pattern_probability
            )
            
            # 제외 번호 필터링
            if exclude_numbers:
                numbers = [n for n in numbers if n not in exclude_numbers]
            
            # 포함 번호 추가
            if include_numbers:
                numbers = list(set(numbers + include_numbers))
            
            # 6개 맞추기
            while len(numbers) < 6:
                pool = [n for n in range(1, 46) 
                        if n not in numbers 
                        and (not exclude_numbers or n not in exclude_numbers)]
                
                if not pool:
                    break
                
                # 빈도 기반 추가
                if in_pattern_probability == 'frequency':
                    weights = [frequency_dict.get(n, 1) for n in pool]
                elif in_pattern_probability == 'inverse':
                    max_freq = max(frequency_dict.get(n, 1) for n in pool)
                    weights = [max_freq - frequency_dict.get(n, 1) + 1 for n in pool]
                else:  # uniform
                    weights = [1] * len(pool)
                
                total = sum(weights)
                probs = [w / total for w in weights]
                
                chosen = np.random.choice(pool, size=1, p=probs)[0]
                numbers.append(chosen)
            
            # 6개 초과 시 제거
            if len(numbers) > 6:
                numbers = numbers[:6]
            
            results.append(sorted(numbers))
        
        return results
    
    def _analyze_range_patterns(
        self,
        analysis_data: pd.DataFrame,
        full_data: pd.DataFrame,
        divisions: int,
        custom_ranges: Optional[Dict],
        top_n: int,
        selection_mode: str
    ) -> Tuple[List[str], callable]:
        """범위 패턴 분석"""
        
        # 구간 정의
        if custom_ranges:
            ranges = custom_ranges
        else:
            ranges = get_default_ranges(divisions)
        
        # 패턴 빈도 집계
        pattern_counter = Counter()
        
        for idx, row in analysis_data.iterrows():
            numbers = [row[f'num{i}'] for i in range(1, 7)]
            pattern = numbers_to_range_pattern(numbers, ranges)
            pattern_counter[pattern] += 1
        
        # 상위 패턴 선택
        if selection_mode == 'normal':
            # 빈도 높은 순
            top_patterns = [pat for pat, _ in pattern_counter.most_common(top_n)]
        else:  # 'inverse'
            # 빈도 낮은 순
            sorted_patterns = sorted(pattern_counter.items(), key=lambda x: x[1])
            top_patterns = [pat for pat, _ in sorted_patterns[:top_n]]
        
        # 패턴 → 번호 변환 함수
        def generate_from_pattern(pattern, freq_dict, prob_mode):
            selected = []
            
            for symbol in pattern:
                start, end = ranges[symbol]
                pool = [n for n in range(start, end+1) if n not in selected]
                
                if not pool:
                    continue
                
                if prob_mode == 'uniform':
                    weights = [1] * len(pool)
                elif prob_mode == 'frequency':
                    weights = [freq_dict.get(n, 1) for n in pool]
                else:  # 'inverse'
                    max_freq = max(freq_dict.get(n, 1) for n in pool)
                    weights = [max_freq - freq_dict.get(n, 1) + 1 for n in pool]
                
                total = sum(weights)
                probs = [w / total for w in weights]
                
                chosen = np.random.choice(pool, size=1, p=probs)[0]
                selected.append(chosen)
            
            return selected
        
        return top_patterns, generate_from_pattern
    
    def _analyze_rank_patterns(
        self,
        analysis_data: pd.DataFrame,
        full_data: pd.DataFrame,
        combo_size: int,
        rank_mode: str,
        rank_window: int,
        top_n: int,
        selection_mode: str
    ) -> Tuple[List[tuple], callable]:
        """순위 패턴 분석"""
        
        # 순위 계산
        ranks_per_round = calculate_frequency_ranks(
            full_data, rank_mode, rank_window
        )
        
        # 순위 조합 패턴 집계
        pattern_counter = Counter()
        
        for idx, row in analysis_data.iterrows():
            numbers = [row[f'num{i}'] for i in range(1, 7)]
            
            rank_dict = ranks_per_round[idx]
            ranks = [rank_dict.get(num, 46) for num in numbers]
            
            # N개 조합 생성
            for combo in combinations(sorted(ranks), combo_size):
                pattern_counter[combo] += 1
        
        # 상위 패턴 선택
        if selection_mode == 'normal':
            top_patterns = [pat for pat, _ in pattern_counter.most_common(top_n)]
        else:  # 'inverse'
            sorted_patterns = sorted(pattern_counter.items(), key=lambda x: x[1])
            top_patterns = [pat for pat, _ in sorted_patterns[:top_n]]
        
        # 최신 회차 순위 (번호 생성용)
        latest_ranks = ranks_per_round[len(full_data) - 1]
        reverse_ranks = {rank: num for num, rank in latest_ranks.items()}
        
        # 패턴 → 번호 변환 함수
        def generate_from_pattern(rank_combo, freq_dict, prob_mode):
            selected = []
            
            # 순위 조합 → 번호
            for target_rank in rank_combo:
                if target_rank in reverse_ranks:
                    selected.append(reverse_ranks[target_rank])
            
            return selected
        
        return top_patterns, generate_from_pattern
    
    def _calculate_frequency(self, data: pd.DataFrame) -> Dict[int, int]:
        """빈도 계산"""
        frequency = Counter()
        
        for idx, row in data.iterrows():
            for i in range(1, 7):
                frequency[row[f'num{i}']] += 1
        
        return dict(frequency)
    
    def calculate_cost(
        self,
        pattern_type: str,
        in_pattern_probability: str,
        range_divisions: int = 5,
        rank_combo_size: int = 3
    ) -> int:
        """파라미터 조합에 따른 비용 계산"""
        
        base_cost = 2
        
        # 역확률 사용 시 +1
        if in_pattern_probability == 'inverse':
            base_cost += 1
        
        # 복잡도에 따라 추가
        if pattern_type == 'range' and range_divisions >= 10:
            base_cost += 1
        
        if pattern_type == 'rank' and rank_combo_size >= 4:
            base_cost += 1
        
        return base_cost
    
    def get_default_parameters(self) -> Dict[str, Any]:
        """기본 파라미터"""
        return {
            'pattern_type': 'range',
            'range_divisions': 5,
            'custom_ranges': None,
            'rank_combo_size': 3,
            'rank_mode': 'cumulative',
            'rank_window': 50,
            'analysis_window_type': 'all',
            'analysis_window_size': None,
            'top_n_patterns': 10,
            'in_pattern_probability': 'frequency',
            'pattern_selection_probability': 'normal'
        }
```

---

## 🎨 UI/UX 설계

### 알고리즘 4 설정 화면 (통합 버전)

```
┌──────────────────────────────────────────┐
│ 패턴 분석 설정                             │
├──────────────────────────────────────────┤
│                                          │
│ 🎯 빠른 설정 (프리셋)                     │
│ ┌──────────┬──────────┐                  │
│ │ 범위기본 │범위역확률⭐│                  │
│ └──────────┴──────────┘                  │
│ ┌──────────┬──────────┐                  │
│ │ 순위기본 │순위역확률⭐│                  │
│ └──────────┴──────────┘                  │
│                                          │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━   │
│                                          │
│ 🎯 패턴 타입                              │
│ ● 범위 패턴 (Range Pattern)              │
│   번호를 구간으로 분할                     │
│                                          │
│ ○ 순위 패턴 (Rank Pattern)               │
│   출현 빈도 순위 조합                      │
│                                          │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━   │
│                                          │
│ 📐 범위 구간 설정                         │
│ ○ 2구간  ○ 3구간  ● 5구간⭐  ○ 10구간    │
│                                          │
│ 📅 분석 범위                              │
│ ● 전체 회차  ○ 최근 [100]회차            │
│                                          │
│ 🔝 상위 패턴 개수                         │
│ ○ 5개  ● 10개⭐  ○ 20개  ○ 30개         │
│                                          │
│ 🎲 패턴 내 번호 선택                      │
│ ○ 균등  ● 빈도 기반  ○ 역확률⭐          │
│                                          │
│ 📈 패턴 선택 방식                         │
│ ● 정확률 (빈도 높은 패턴)                 │
│ ○ 역확률 (빈도 낮은 패턴)                 │
│                                          │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━   │
│                                          │
│ 💰 예상 비용: 2 코인/세트                │
│                                          │
│ [이전]                     [번호 생성 →] │
└──────────────────────────────────────────┘
```

---

## 📊 Trial 원본 알고리즘 매핑

| Trial | 설명 | 알고리즘 4 파라미터 |
|-------|------|-------------------|
| **Trial04** | ABCDE 범위 패턴 | `pattern_type='range', range_divisions=5, in_pattern_probability='frequency'` |
| **Trial04 고급** | ABCDE 범위 + 역확률 | `pattern_type='range', range_divisions=5, in_pattern_probability='inverse'` |
| **Trial05** | 3개 순위 조합 패턴 | `pattern_type='rank', rank_combo_size=3, in_pattern_probability='frequency'` |
| **Trial05 고급** | 3개 순위 + 역확률 | `pattern_type='rank', rank_combo_size=3, in_pattern_probability='inverse'` |

✅ **하나의 알고리즘 4로 Trial04/Trial05 모두 커버!**

---

## 📊 전체 알고리즘 체계 (최종)

| ID | 이름 | 설명 | 비용 |
|----|------|------|------|
| **1** | 순수 랜덤 | Quick Pick | 무료 |
| **2** | 고급 빈도 분석 | 빈도 + 필터 + 역확률 | 1~3코인 |
| **3** | LSTM 고급 분석 | 학습 방식 × 확률 방식 | 2~5코인 |
| **4** | 패턴 분석 | 범위/순위 패턴 | 2~4코인 |

---

## ✅ 체크리스트

### 설계 완료
- [x] 패턴 분석 통합 설계
- [x] 범위 패턴 상세 명세
- [x] 순위 패턴 상세 명세
- [x] 파라미터 구조 정의
- [x] UI/UX 설계
- [x] Trial04/Trial05 매핑
- [x] 비용 정책

### 구현 대기
- [ ] `algorithm_04_advanced_pattern.py` 작성
- [ ] 프론트엔드 파라미터 입력 UI
- [ ] 프리셋 기능
- [ ] 패턴 시각화

---

## 💡 추가 고려사항

### 1. 패턴 시각화
- 범위 패턴: 막대 그래프 (A~E 분포)
- 순위 패턴: 히트맵 (순위 조합 빈도)

### 2. 패턴 설명
- 각 패턴의 과거 출현 횟수 표시
- "이 패턴은 최근 20회차에 3번 나왔습니다"

### 3. 성능 최적화
- 패턴 분석 결과 캐싱
- 자주 사용하는 패턴 사전 계산

---

**작성**: 2026-01-07 EST  
**버전**: 1.0 (통합 버전)  
**다음 단계**: 앙상블 알고리즘 설계 (Phase 3)

