# 알고리즘 재설계 명세서 - LSTM 편

**작성일**: 2026-01-07  
**목적**: Trial01/Trial02 LSTM 아이디어 기반 통합 알고리즘 설계  
**참조**: 010.1_Algorithm_Redesign.md (빈도 기반 알고리즘 설계)

---

## 📋 설계 원칙

### 1. 빈도 기반 알고리즘과 동일한 철학
- **하나의 알고리즘, 여러 파라미터 조합**
- 프리셋 + 커스텀 지원
- 사용자 친화적 인터페이스

### 2. Trial01/Trial02 핵심 보존
- **Non-Cumulative vs Cumulative Learning** (학습 전략)
- **Normal vs Inverse Probability** (샘플링 전략)
- **역확률 샘플링**: 빈도 기반과 동일한 철학 (평균 회귀)

### 3. 일관성 유지
- 빈도 기반 알고리즘(알고리즘 2)과 동일한 인터페이스
- 제외 필터, 확률 조정 등 공통 옵션 지원

---

## 🎯 알고리즘 체계

### Phase 1: 통합 전 구조 (기존)

| 알고리즘 ID | 이름 | 학습 방식 | 확률 방식 | 비용 |
|------------|------|----------|----------|------|
| 알고리즘 2 | LSTM 기본 | Non-Cumulative | Normal | 3코인 |
| 알고리즘 3 | LSTM + 역확률 | Non-Cumulative | Inverse | 4코인 |
| 알고리즘 4 | LSTM 누적 | Cumulative | Normal | 2코인 |
| 알고리즘 5 | LSTM 누적 + 역확률 | Cumulative | Inverse | 5코인 |

**문제점**: 4개 알고리즘이 사실상 파라미터 조합일 뿐

---

### Phase 2: 통합 후 구조 (제안) ⭐

#### **알고리즘 3: LSTM 고급 분석 (Advanced LSTM)**

**설명**: PyTorch LSTM으로 시계열 패턴을 학습하고, 다양한 학습 전략과 확률 변형을 적용할 수 있는 고도로 커스터마이즈 가능한 딥러닝 알고리즘

**핵심 차별화 요소**:
1. **학습 방식 선택** (Non-Cumulative vs Cumulative)
2. **역확률 샘플링** (빈도 기반과 동일한 철학)
3. **모델 구조 커스터마이징** (hidden_size, num_layers, dropout)
4. **학습 하이퍼파라미터 조정** (epochs, learning_rate, batch_size)
5. **직전 회차 확률 할인** (빈도 기반과 동일)

**파라미터 조합**:
- `학습=Non-Cumulative + 확률=Normal` → 기존 알고리즘 2
- `학습=Non-Cumulative + 확률=Inverse` → 기존 알고리즘 3 ⭐
- `학습=Cumulative + 확률=Normal` → 기존 알고리즘 4
- `학습=Cumulative + 확률=Inverse` → 기존 알고리즘 5 ⭐⭐

**비용**: 2~5코인 (파라미터 조합에 따라)

---

## 📊 파라미터 구조

### A. 학습 방식 (Learning Mode)

**설명**: 모델 학습 전략 선택

**옵션**:

#### **A-1. Non-Cumulative Learning (비누적 학습)**

**정의**: 매번 전체 데이터로 처음부터 새로 학습

**특징**:
- ✅ 깨끗한 상태로 시작 (편향 없음)
- ✅ 재현성 보장
- ❌ 학습 시간 오래 걸림
- ❌ 이전 학습 활용 불가

**용도**: 
- 단일 예측 (사용자 요청 시)
- 실험/검증 단계

**UI 라벨**: "처음부터 학습 (느림, 정확)"

---

#### **A-2. Cumulative Learning (누적 학습)**

**정의**: 이전 모델 저장 → 신규 데이터만 추가 학습

**특징**:
- ✅ 학습 속도 빠름
- ✅ 장기 패턴 누적
- ❌ Overfitting 위험
- ❌ 재현성 낮음

**용도**:
- 배치 예측 (자동 업데이트)
- 프로덕션 최적화

**UI 라벨**: "이어서 학습 (빠름, 누적)"

---

**UI 예시**:
```
┌─────────────────────────────────────┐
│ 🎓 학습 방식                          │
├─────────────────────────────────────┤
│ ● 처음부터 학습 (Non-Cumulative)      │
│   매번 전체 데이터로 새로 학습         │
│   ✓ 편향 없음  ✗ 느림 (3코인)        │
│                                     │
│ ○ 이어서 학습 (Cumulative)            │
│   이전 모델에 신규 데이터만 추가       │
│   ✓ 빠름  ✗ 과적합 위험 (2코인)      │
└─────────────────────────────────────┘
```

---

### B. 확률 적용 방식 (Probability Mode)

**설명**: LSTM 출력 확률을 샘플링에 사용하는 방식 (빈도 기반과 동일!)

**옵션**:

#### **B-1. 정확률 (Normal Probability)**

**정의**: LSTM이 출력한 확률을 그대로 사용

**수식**:
```
P(번호) = LSTM_output(번호)
```

**예시**:
- LSTM: 12번 → 15% 확률
- LSTM: 43번 → 3% 확률
- **12번이 더 선택될 확률 높음**

**특징**: "AI가 예측한 번호를 따른다"

---

#### **B-2. 역확률 (Inverse Probability)** ⭐

**정의**: LSTM 출력 확률을 역전시켜 샘플링 (빈도 기반과 동일한 철학!)

**수식**:
```
max_prob = max(LSTM_output)
inverse_prob(번호) = max_prob - LSTM_output(번호) + epsilon

P(번호) = inverse_prob(번호) / sum(inverse_prob)
```

**예시**:
- LSTM: 12번 → 15% → 역확률: 낮은 가중치
- LSTM: 43번 → 3% → 역확률: 높은 가중치
- **43번이 더 선택될 확률 높음**

**특징**: 
- "AI가 예측한 번호는 이미 충분히 나왔을 것" (평균 회귀)
- 빈도 기반 역확률과 동일한 철학
- Trial02의 핵심 차별화!

**UI 예시**:
```
┌─────────────────────────────────────┐
│ 🎲 확률 적용 방식                     │
├─────────────────────────────────────┤
│ ● 정확률 (AI 예측 따름)               │
│   LSTM 출력 확률 그대로 사용          │
│   "AI가 예측한 번호를 선택"           │
│                                     │
│ ○ 역확률 (평균 회귀) ⭐               │
│   LSTM 확률을 역전시켜 샘플링         │
│   "AI가 예측한 번호는 이미 나왔을 것" │
│   (빈도 기반 역확률과 동일한 철학)     │
└─────────────────────────────────────┘
```

---

### C. 학습 범위 (Training Window)

**설명**: LSTM 학습에 사용할 과거 회차 범위

**옵션**:
- **Sliding Window Size**: 10 ~ 500 (기본값: 100)
  - 프리셋 버튼:
    - `50회` (단기 패턴)
    - `100회` (균형) ⭐
    - `200회` (장기 패턴)
  - 직접 입력: 슬라이더 (10 ~ 500)

**UI 예시**:
```
┌─────────────────────────────────────┐
│ 📊 학습 범위 (Window Size)            │
├─────────────────────────────────────┤
│   [50] [100⭐] [200]                 │
│                                     │
│   ──────────●───────────            │
│   10       100       500            │
│                                     │
│   직접 입력: [____] 회차             │
└─────────────────────────────────────┘
```

---

### D. 모델 구조 (Model Architecture)

**설명**: LSTM 모델의 내부 구조 설정

**파라미터**:

1. **Hidden Size** (은닉층 크기)
   - 옵션: 32, 64, 128 ⭐, 256
   - 설명: 클수록 복잡한 패턴 학습 가능, 과적합 위험 증가

2. **Num Layers** (LSTM 층 개수)
   - 옵션: 1, 2 ⭐, 3
   - 설명: 많을수록 깊은 학습, 학습 시간 증가

3. **Dropout** (드롭아웃)
   - 범위: 0.0 ~ 0.5 (기본값: 0.2)
   - 설명: 과적합 방지, 높을수록 강한 정규화

**UI 예시**:
```
┌─────────────────────────────────────┐
│ 🧠 모델 구조 (고급)                   │
├─────────────────────────────────────┤
│ Hidden Size:                        │
│   ○ 32  ○ 64  ● 128  ○ 256         │
│                                     │
│ Layers:                             │
│   ○ 1  ● 2  ○ 3                    │
│                                     │
│ Dropout: [0.2] (0.0~0.5)            │
└─────────────────────────────────────┘
```

---

### E. 학습 설정 (Training Settings)

**설명**: LSTM 학습 하이퍼파라미터

**파라미터**:

1. **Num Epochs** (학습 반복 횟수)
   - 옵션: 20, 50 ⭐, 100, 200
   - 설명: 많을수록 정확, 시간 증가

2. **Learning Rate** (학습률)
   - 옵션: 0.0001, 0.001 ⭐, 0.01
   - 설명: 학습 속도 조절

3. **Batch Size** (배치 크기)
   - 옵션: 16, 32 ⭐, 64
   - 설명: 메모리 효율성

**UI 예시**:
```
┌─────────────────────────────────────┐
│ ⚙️ 학습 설정 (고급)                   │
├─────────────────────────────────────┤
│ Epochs:                             │
│   ○ 20  ● 50  ○ 100  ○ 200         │
│                                     │
│ Learning Rate:                      │
│   ○ 0.0001  ● 0.001  ○ 0.01        │
│                                     │
│ Batch Size:                         │
│   ○ 16  ● 32  ○ 64                 │
└─────────────────────────────────────┘
```

---

### F. 추가 옵션 (Additional Options)

**설명**: 빈도 기반 알고리즘과 동일한 추가 옵션

#### **F-1. 직전 회차 확률 할인** 💸

**정의**: 직전 회차 출현 번호의 선택 확률 감소 (빈도 기반과 동일!)

**옵션**:
- 적용 안 함 (기본값)
- 프리셋 할인율: 1/2, 1/3, 1/4
- 커스텀: 0.1 ~ 1.0

**UI 예시**:
```
┌─────────────────────────────────────┐
│ 💸 직전 회차 확률 할인                │
├─────────────────────────────────────┤
│ □ 직전 회차 출현 번호 확률 조정       │
│                                     │
│   할인율:                            │
│   ○ 1/2  ○ 1/3  ○ 1/4  ● 커스텀    │
│   [0.5] (0.1~1.0)                   │
└─────────────────────────────────────┘
```

---

#### **F-2. 온도 파라미터** 🌡️

**정의**: 확률 분포의 날카로움 조정 (빈도 기반과 동일!)

**수식**:
```
P_adjusted = P^(1/T) / sum(P^(1/T))

T = 0.5: 확률 차이 극대화
T = 1.0: 원본 유지 (기본값)
T = 2.0: 확률 차이 완화
```

**UI 예시**:
```
┌─────────────────────────────────────┐
│ 🌡️ 온도 조정 (고급)                  │
├─────────────────────────────────────┤
│ □ 확률 분포 조정                     │
│                                     │
│   Temperature: [1.0] (0.1~3.0)      │
│                                     │
│   낮음 ← [____●____] → 높음         │
│   0.5    1.0    2.0                │
└─────────────────────────────────────┘
```

---

## 🎯 프리셋 조합

### 기본 프리셋 (4개)

| 프리셋 이름 | 학습 방식 | 확률 방식 | 파라미터 | 비용 |
|------------|----------|----------|---------|------|
| **기본 LSTM** | Non-Cumulative | Normal | `learning_mode='non-cumulative', probability_mode='normal'` | 3코인 |
| **LSTM 역확률** ⭐ | Non-Cumulative | Inverse | `learning_mode='non-cumulative', probability_mode='inverse'` | 4코인 |
| **LSTM 누적** | Cumulative | Normal | `learning_mode='cumulative', probability_mode='normal'` | 2코인 |
| **LSTM 누적+역** ⭐⭐ | Cumulative | Inverse | `learning_mode='cumulative', probability_mode='inverse'` | 5코인 |

### 고급 프리셋 (추가)

| 프리셋 이름 | 설명 | 파라미터 |
|------------|------|---------|
| **빠른 실험** | 빠른 테스트용 | `window_size=50, num_epochs=20, hidden_size=64` |
| **균형 학습** ⭐ | 기본 추천 | `window_size=100, num_epochs=50, hidden_size=128` |
| **정밀 학습** | 높은 정확도 | `window_size=200, num_epochs=100, hidden_size=256` |
| **최근 트렌드** | 단기 패턴 | `window_size=30, num_epochs=50, hidden_size=128` |

---

## 🎨 UI/UX 설계

### 알고리즘 3 설정 화면 (통합 버전)

```
┌──────────────────────────────────────────┐
│ LSTM 고급 분석 설정                        │
├──────────────────────────────────────────┤
│                                          │
│ 🎯 빠른 설정 (프리셋)                     │
│ ┌──────────┬──────────┐                  │
│ │ 기본LSTM │LSTM역확률⭐│                  │
│ └──────────┴──────────┘                  │
│ ┌──────────┬──────────┐                  │
│ │LSTM 누적 │누적+역⭐⭐│                  │
│ └──────────┴──────────┘                  │
│                                          │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━   │
│                                          │
│ 🎓 학습 방식                              │
│ ● 처음부터 학습 (Non-Cumulative)          │
│   ✓ 편향 없음  ✗ 느림                    │
│                                          │
│ ○ 이어서 학습 (Cumulative)                │
│   ✓ 빠름  ✗ 과적합 위험                  │
│                                          │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━   │
│                                          │
│ 🎲 확률 적용 방식                         │
│ ● 정확률 (AI 예측 따름)                   │
│   "AI가 예측한 번호를 선택"               │
│                                          │
│ ○ 역확률 (평균 회귀) ⭐                   │
│   "AI 예측 번호는 이미 나왔을 것"         │
│                                          │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━   │
│                                          │
│ 📊 학습 범위                              │
│   [50] [100⭐] [200]  또는 [___]         │
│                                          │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━   │
│                                          │
│ ▼ 고급 설정 (선택사항)                    │
│                                          │
│ 🧠 모델 구조                              │
│ Hidden: [○32 ○64 ●128 ○256]            │
│ Layers: [○1 ●2 ○3]                      │
│ Dropout: [0.2]                          │
│                                          │
│ ⚙️ 학습 설정                              │
│ Epochs: [○20 ●50 ○100 ○200]            │
│ LR: [○0.0001 ●0.001 ○0.01]             │
│ Batch: [○16 ●32 ○64]                    │
│                                          │
│ 💡 추가 옵션                              │
│ □ 직전 회차 확률 할인 (0.5)              │
│ □ 온도 조정 (1.0)                        │
│                                          │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━   │
│                                          │
│ 💰 예상 비용: 3 코인/세트                │
│                                          │
│ [이전]                     [번호 생성 →] │
└──────────────────────────────────────────┘
```

---

## 💰 비용 정책

### 기본 비용 산정 (학습 방식 + 확률 방식)

| 학습 방식 | 확률 방식 | 기본 비용 | 설명 |
|----------|----------|---------|------|
| Non-Cumulative | Normal | 3코인 | 전체 학습 + 정확률 |
| Non-Cumulative | Inverse ⭐ | 4코인 | 전체 학습 + 역확률 (고급) |
| Cumulative | Normal | 2코인 | 증분 학습 + 정확률 (빠름) |
| Cumulative | Inverse ⭐⭐ | 5코인 | 증분 학습 + 역확률 (최고급) |

### 추가 비용 (옵션)

| 옵션 | 추가 비용 | 설명 |
|-----|----------|------|
| Hidden Size 256 | +1코인 | 대형 모델 |
| Epochs 100+ | +1코인 | 장시간 학습 |
| 정밀 학습 프리셋 | +2코인 | 최고 정확도 |

---

## 📝 완전 구현 코드

### 통합 파라미터 구조

```python
class LSTMParams:
    # A. 학습 방식
    learning_mode: Literal['non-cumulative', 'cumulative'] = 'non-cumulative'
    
    # B. 확률 방식
    probability_mode: Literal['normal', 'inverse'] = 'normal'
    
    # C. 학습 범위
    window_size: int = 100  # 10~500
    
    # D. 모델 구조
    hidden_size: int = 128  # 32, 64, 128, 256
    num_layers: int = 2  # 1, 2, 3
    dropout: float = 0.2  # 0.0~0.5
    
    # E. 학습 설정
    num_epochs: int = 50  # 20, 50, 100, 200
    learning_rate: float = 0.001  # 0.0001, 0.001, 0.01
    batch_size: int = 32  # 16, 32, 64
    
    # F. 추가 옵션
    apply_recent_penalty: bool = False
    penalty_rate: float = 0.5  # 0.1~1.0
    temperature: float = 1.0  # 0.1~3.0
```

---

### 통합 알고리즘 클래스

```python
"""
Algorithm 3: LSTM 고급 분석 (통합 버전)

학습 방식 × 확률 방식 = 4가지 조합을 하나로 통합

2026-01-07 EST - 초기 설계 (통합 버전)
"""

import torch
import torch.nn as nn
import torch.optim as optim
import numpy as np
import pandas as pd
from typing import List, Dict, Optional, Any, Literal
import os
import glob
from datetime import datetime

from app.algorithms.base import LottoAlgorithm


# =====================================
# LSTM 모델 정의 (동일)
# =====================================

class LottoLSTM(nn.Module):
    """로또 번호 예측 LSTM 모델"""
    
    def __init__(
        self,
        input_size: int = 45,
        hidden_size: int = 128,
        num_layers: int = 2,
        dropout: float = 0.2
    ):
        super(LottoLSTM, self).__init__()
        
        self.lstm = nn.LSTM(
            input_size=input_size,
            hidden_size=hidden_size,
            num_layers=num_layers,
            dropout=dropout if num_layers > 1 else 0.0,
            batch_first=True
        )
        
        self.fc = nn.Linear(hidden_size, input_size)
        self.sigmoid = nn.Sigmoid()
    
    def forward(self, x):
        out, _ = self.lstm(x)
        out = out[:, -1, :]
        out = self.fc(out)
        out = self.sigmoid(out)
        return out


# =====================================
# 데이터 전처리 (동일)
# =====================================

def numbers_to_onehot(numbers: List[int]) -> np.ndarray:
    """로또 번호 → 45차원 원-핫 벡터"""
    onehot = np.zeros(45, dtype=np.float32)
    for num in numbers:
        onehot[num - 1] = 1.0
    return onehot


def create_sequences(
    historical_data: pd.DataFrame,
    window_size: int
) -> tuple[np.ndarray, np.ndarray]:
    """Sliding window로 학습 데이터 생성"""
    onehots = []
    for idx, row in historical_data.iterrows():
        numbers = [row[f'num{i}'] for i in range(1, 7)]
        onehot = numbers_to_onehot(numbers)
        onehots.append(onehot)
    
    onehots = np.array(onehots)
    
    X, y = [], []
    for i in range(len(onehots) - window_size):
        X.append(onehots[i:i+window_size])
        y.append(onehots[i+window_size])
    
    return np.array(X, dtype=np.float32), np.array(y, dtype=np.float32)


# =====================================
# 학습 함수 (동일)
# =====================================

def train_lstm(
    model: LottoLSTM,
    X_train: np.ndarray,
    y_train: np.ndarray,
    num_epochs: int = 50,
    learning_rate: float = 0.001,
    batch_size: int = 32,
    device: str = 'cpu'
) -> LottoLSTM:
    """LSTM 모델 학습"""
    model = model.to(device)
    model.train()
    
    criterion = nn.BCELoss()
    optimizer = optim.Adam(model.parameters(), lr=learning_rate)
    
    X_tensor = torch.FloatTensor(X_train).to(device)
    y_tensor = torch.FloatTensor(y_train).to(device)
    
    n_samples = len(X_train)
    
    for epoch in range(num_epochs):
        epoch_loss = 0.0
        indices = np.random.permutation(n_samples)
        
        for i in range(0, n_samples, batch_size):
            batch_indices = indices[i:i+batch_size]
            X_batch = X_tensor[batch_indices]
            y_batch = y_tensor[batch_indices]
            
            outputs = model(X_batch)
            loss = criterion(outputs, y_batch)
            
            optimizer.zero_grad()
            loss.backward()
            optimizer.step()
            
            epoch_loss += loss.item()
        
        if (epoch + 1) % 10 == 0:
            avg_loss = epoch_loss / (n_samples // batch_size)
            print(f"[LSTM] Epoch {epoch+1}/{num_epochs}, Loss: {avg_loss:.4f}")
    
    return model


def predict_probabilities(
    model: LottoLSTM,
    historical_data: pd.DataFrame,
    window_size: int,
    device: str = 'cpu'
) -> Dict[int, float]:
    """LSTM으로 확률 예측"""
    model = model.to(device)
    model.eval()
    
    recent = historical_data.tail(window_size)
    
    onehots = []
    for idx, row in recent.iterrows():
        numbers = [row[f'num{i}'] for i in range(1, 7)]
        onehot = numbers_to_onehot(numbers)
        onehots.append(onehot)
    
    X = np.array([onehots], dtype=np.float32)
    X_tensor = torch.FloatTensor(X).to(device)
    
    with torch.no_grad():
        probs = model(X_tensor)
        probs = probs.cpu().numpy()[0]
    
    probabilities = {i+1: float(probs[i]) for i in range(45)}
    
    return probabilities


# =====================================
# 모델 저장/로드 (Cumulative용)
# =====================================

MODEL_DIR = "backend/app/models/lstm_checkpoints"
os.makedirs(MODEL_DIR, exist_ok=True)


def save_model(
    model: LottoLSTM,
    round_number: int,
    hidden_size: int,
    num_layers: int,
    dropout: float
):
    """모델 체크포인트 저장"""
    path = f"{MODEL_DIR}/lstm_model_round_{round_number}.pth"
    
    torch.save({
        'model_state_dict': model.state_dict(),
        'round_number': round_number,
        'hidden_size': hidden_size,
        'num_layers': num_layers,
        'dropout': dropout,
        'timestamp': datetime.now().isoformat()
    }, path)
    
    print(f"[LSTM] 모델 저장: {path}")


def load_latest_model(
    hidden_size: int,
    num_layers: int,
    dropout: float
) -> tuple[Optional[LottoLSTM], Optional[int]]:
    """가장 최신 모델 로드"""
    checkpoints = glob.glob(f"{MODEL_DIR}/lstm_model_round_*.pth")
    
    if not checkpoints:
        return None, None
    
    latest = max(checkpoints, 
                 key=lambda x: int(x.split('_')[-1].split('.')[0]))
    
    checkpoint = torch.load(latest, map_location='cpu')
    round_number = checkpoint['round_number']
    
    model = LottoLSTM(
        input_size=45,
        hidden_size=checkpoint.get('hidden_size', hidden_size),
        num_layers=checkpoint.get('num_layers', num_layers),
        dropout=checkpoint.get('dropout', dropout)
    )
    model.load_state_dict(checkpoint['model_state_dict'])
    
    print(f"[LSTM] 모델 로드: 회차 {round_number}")
    
    return model, round_number


# =====================================
# 통합 알고리즘 클래스 ⭐
# =====================================

class AdvancedLSTMAlgorithm(LottoAlgorithm):
    """
    알고리즘 3: LSTM 고급 분석 (통합 버전)
    
    학습 방식 × 확률 방식 = 4가지 조합
    """
    
    def __init__(self):
        super().__init__(
            algorithm_id=3,
            name="LSTM 고급 분석 (Advanced LSTM)",
            description="LSTM 학습 + 다양한 전략 (학습 방식 × 확률 방식)",
            cost_per_set=3  # 기본값 (파라미터로 조정)
        )
    
    def generate_numbers(
        self,
        historical_data: Optional[pd.DataFrame],
        n_sets: int = 5,
        exclude_numbers: Optional[List[int]] = None,
        include_numbers: Optional[List[int]] = None,
        
        # A. 학습 방식 ⭐
        learning_mode: Literal['non-cumulative', 'cumulative'] = 'non-cumulative',
        
        # B. 확률 방식 ⭐
        probability_mode: Literal['normal', 'inverse'] = 'normal',
        
        # C. 학습 범위
        window_size: int = 100,
        
        # D. 모델 구조
        hidden_size: int = 128,
        num_layers: int = 2,
        dropout: float = 0.2,
        
        # E. 학습 설정
        num_epochs: int = 50,
        learning_rate: float = 0.001,
        batch_size: int = 32,
        
        # F. 추가 옵션
        apply_recent_penalty: bool = False,
        penalty_rate: float = 0.5,
        temperature: float = 1.0,
        
        # 디바이스
        device: str = 'cpu',
        
        **kwargs
    ) -> List[List[int]]:
        """LSTM 고급 분석 번호 생성 (통합 버전)"""
        
        # 파라미터 검증
        valid, error_msg = self.validate_parameters(
            n_sets, exclude_numbers, include_numbers
        )
        if not valid:
            raise ValueError(error_msg)
        
        if historical_data is None or len(historical_data) < window_size + 10:
            raise ValueError(f"최소 {window_size + 10}회차 이상의 데이터 필요")
        
        # 현재 최신 회차
        current_round = historical_data.iloc[-1]['round']
        
        # ===== 1. 학습 방식에 따라 분기 =====
        
        if learning_mode == 'non-cumulative':
            # Non-Cumulative: 전체 학습
            print(f"[알고리즘 3] Non-Cumulative 학습 시작")
            
            X_train, y_train = create_sequences(historical_data, window_size)
            
            model = LottoLSTM(
                input_size=45,
                hidden_size=hidden_size,
                num_layers=num_layers,
                dropout=dropout
            )
            
            model = train_lstm(
                model, X_train, y_train,
                num_epochs=num_epochs,
                learning_rate=learning_rate,
                batch_size=batch_size,
                device=device
            )
        
        else:  # 'cumulative'
            # Cumulative: 증분 학습
            print(f"[알고리즘 3] Cumulative 학습 시작")
            
            # 기존 모델 로드
            model, last_round = load_latest_model(
                hidden_size, num_layers, dropout
            )
            
            if model is None:
                # 처음 학습
                print(f"[알고리즘 3] 초기 학습 (전체 데이터)")
                model = LottoLSTM(
                    input_size=45,
                    hidden_size=hidden_size,
                    num_layers=num_layers,
                    dropout=dropout
                )
                training_data = historical_data
                last_round = 0
            else:
                # 증분 학습
                print(f"[알고리즘 3] 증분 학습 (회차 {last_round+1} ~ {current_round})")
                training_data = historical_data[
                    historical_data['round'] > last_round
                ]
            
            # 신규 데이터로 학습
            if len(training_data) >= window_size + 1:
                X_train, y_train = create_sequences(training_data, window_size)
                
                if len(X_train) > 0:
                    # Cumulative는 epochs 짧게
                    model = train_lstm(
                        model, X_train, y_train,
                        num_epochs=min(num_epochs, 20),
                        learning_rate=learning_rate,
                        batch_size=batch_size,
                        device=device
                    )
                    
                    # 모델 저장
                    save_model(
                        model, current_round,
                        hidden_size, num_layers, dropout
                    )
        
        # ===== 2. 확률 예측 =====
        probabilities = predict_probabilities(
            model, historical_data, window_size, device
        )
        
        # 제외 번호 필터링
        if exclude_numbers:
            for num in exclude_numbers:
                probabilities.pop(num, None)
        
        if include_numbers:
            for num in include_numbers:
                probabilities.pop(num, None)
        
        # ===== 3. 확률 방식에 따라 분기 ⭐ =====
        
        if probability_mode == 'inverse':
            # 역확률 변환
            probabilities = self._apply_inverse_probability(probabilities)
        
        # ===== 4. 추가 옵션 적용 =====
        
        # 직전 회차 패널티
        if apply_recent_penalty:
            probabilities = self._apply_recent_draw_penalty(
                probabilities, historical_data, penalty_rate
            )
        
        # 온도 조정
        probabilities = self._apply_temperature(probabilities, temperature)
        
        # ===== 5. 번호 생성 =====
        results = []
        numbers_list = list(probabilities.keys())
        probs_list = list(probabilities.values())
        
        # 정규화
        probs_sum = sum(probs_list)
        probs_list = [p / probs_sum for p in probs_list]
        
        for _ in range(n_sets):
            selected = []
            
            if include_numbers:
                selected.extend(include_numbers)
            
            remaining = 6 - len(selected)
            if remaining > 0:
                chosen = np.random.choice(
                    numbers_list,
                    size=remaining,
                    replace=False,
                    p=probs_list
                )
                selected.extend(chosen.tolist())
            
            results.append(sorted(selected))
        
        return results
    
    def _apply_inverse_probability(
        self,
        probabilities: Dict[int, float]
    ) -> Dict[int, float]:
        """
        역확률 변환 ⭐
        
        LSTM 확률 높음 → 역확률 낮음
        LSTM 확률 낮음 → 역확률 높음
        """
        if not probabilities:
            return probabilities
        
        max_prob = max(probabilities.values())
        epsilon = 0.01
        
        inverse = {
            num: max_prob - prob + epsilon 
            for num, prob in probabilities.items()
        }
        
        total = sum(inverse.values())
        inverse = {num: inv / total for num, inv in inverse.items()}
        
        return inverse
    
    def _apply_recent_draw_penalty(
        self,
        probabilities: Dict[int, float],
        historical_data: pd.DataFrame,
        penalty_rate: float
    ) -> Dict[int, float]:
        """직전 회차 확률 할인 (빈도 기반과 동일)"""
        if len(historical_data) < 1:
            return probabilities
        
        latest_draw = historical_data.tail(1).iloc[0]
        latest_numbers = set()
        for i in range(1, 7):
            latest_numbers.add(latest_draw[f'num{i}'])
        
        adjusted = probabilities.copy()
        for num in latest_numbers:
            if num in adjusted:
                adjusted[num] *= penalty_rate
        
        total = sum(adjusted.values())
        adjusted = {num: prob / total for num, prob in adjusted.items()}
        
        return adjusted
    
    def _apply_temperature(
        self,
        probabilities: Dict[int, float],
        temperature: float
    ) -> Dict[int, float]:
        """온도 조정 (빈도 기반과 동일)"""
        if temperature == 1.0:
            return probabilities
        
        adjusted = {num: p ** (1/temperature) 
                    for num, p in probabilities.items()}
        
        total = sum(adjusted.values())
        return {num: adj / total for num, adj in adjusted.items()}
    
    def calculate_cost(
        self,
        learning_mode: str,
        probability_mode: str,
        hidden_size: int = 128,
        num_epochs: int = 50
    ) -> int:
        """
        파라미터 조합에 따른 비용 계산
        
        Returns:
            코인/세트
        """
        # 기본 비용
        base_cost = {
            ('non-cumulative', 'normal'): 3,
            ('non-cumulative', 'inverse'): 4,
            ('cumulative', 'normal'): 2,
            ('cumulative', 'inverse'): 5
        }
        
        cost = base_cost.get((learning_mode, probability_mode), 3)
        
        # 추가 비용
        if hidden_size >= 256:
            cost += 1
        if num_epochs >= 100:
            cost += 1
        
        return cost
    
    def get_default_parameters(self) -> Dict[str, Any]:
        """기본 파라미터"""
        return {
            'learning_mode': 'non-cumulative',
            'probability_mode': 'normal',
            'window_size': 100,
            'hidden_size': 128,
            'num_layers': 2,
            'dropout': 0.2,
            'num_epochs': 50,
            'learning_rate': 0.001,
            'batch_size': 32,
            'apply_recent_penalty': False,
            'penalty_rate': 0.5,
            'temperature': 1.0,
            'device': 'cpu'
        }
```

---

## 📊 Trial02 원본 알고리즘 매핑

| Trial02 | 설명 | 알고리즘 3 파라미터 |
|---------|------|-------------------|
| **알고리즘 2** | LSTM 비누적 | `learning_mode='non-cumulative', probability_mode='normal'` |
| **알고리즘 3** ⭐ | 알고리즘 2 + 역확률 | `learning_mode='non-cumulative', probability_mode='inverse'` |
| **알고리즘 4** | LSTM 누적 | `learning_mode='cumulative', probability_mode='normal'` |
| **알고리즘 5** ⭐⭐ | 알고리즘 4 + 역확률 | `learning_mode='cumulative', probability_mode='inverse'` |

✅ **하나의 알고리즘 3으로 Trial02의 알고리즘 2,3,4,5 모두 커버!**

---

## 📊 전체 알고리즘 체계 (최종)

| ID | 이름 | 설명 | 비용 |
|----|------|------|------|
| **1** | 순수 랜덤 | Quick Pick, Baseline | 무료 |
| **2** | 고급 빈도 분석 | 빈도 기반 + 다양한 필터 + 역확률 | 1~3코인 |
| **3** | LSTM 고급 분석 | LSTM + 학습 방식 × 확률 방식 | 2~5코인 |

---

## ✅ 체크리스트

### 설계 완료
- [x] LSTM 알고리즘 통합 설계
- [x] 파라미터 구조 정의
- [x] UI/UX 설계
- [x] Trial02 매핑
- [x] 비용 정책

### 구현 대기
- [ ] `algorithm_03_advanced_lstm.py` 작성
- [ ] 프론트엔드 파라미터 입력 UI
- [ ] 프리셋 기능
- [ ] 모델 체크포인트 관리
- [ ] GPU 지원 (CUDA)

---

## 💡 추가 고려사항

### 1. 학습 시간 표시
- Non-Cumulative: "약 30초 소요 예상"
- Cumulative: "약 5초 소요 예상"

### 2. 모델 관리
- Cumulative 모델 자동 백업
- 오래된 체크포인트 자동 삭제 (최근 10개만 유지)

### 3. 에러 핸들링
- 학습 실패 시 자동 재시도 (최대 3회)
- GPU 메모리 부족 시 CPU로 fallback

---

**작성**: 2026-01-07 EST  
**버전**: 1.0 (통합 버전)  
**다음 단계**: 패턴 분석 알고리즘 설계 (Phase 2+)

